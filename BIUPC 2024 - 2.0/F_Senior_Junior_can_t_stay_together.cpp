#include<bits/stdc++.h>
using namespace std;

int  main(){
    int n ; cin >> n;
    if(n <= 50 and n >= 48) {
        cout<<"Probin"<<endl;
    }
    else if(n <= 51 and n >= 53) {
        cout<<"Nobin"<<endl;
    }
    else {
        cout<<"Bohiragoto"<<endl;
    }
}