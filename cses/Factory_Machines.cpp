#include<bits/stdc++.h>
using namespace std;
#define int long long
#define endl '\n'
#define print(x) cout<<x<<'\n'
int n,k;
bool call(int mid){
    
}

int32_t main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0); cout.tie(0);
    cin>>n>>k;
    vector<int>v(n);
    for(auto &u : v) cin>>u;

    int l=0,r=1e9;
    while(l<r){
        int mid = (l+r)/2;
        if(call(mid)){

        }
    }

}